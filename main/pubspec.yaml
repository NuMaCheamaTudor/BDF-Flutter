name: main
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  concentric_transition: ^1.0.3

  # 🔔 Plugin pentru notificări locale
  flutter_local_notifications: ^19.2.0

  # 🕒 Timezone pentru notificări programate
  timezone: ^0.10.1

  # 🔐 Permisiuni runtime (Android 13+)
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true



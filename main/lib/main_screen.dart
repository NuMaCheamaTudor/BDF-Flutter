
import 'package:flutter/material.dart';

class MainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Recapata controlul")),
      body: const Center(child: Text("Fii stapan pe tine")),
    );
  }
}
